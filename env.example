# =============================================================================
# Follower Tracker 环境配置文件
# 复制此文件为 .env 并根据需要修改配置
# =============================================================================

# 应用配置
APP_NAME=Follower Tracker
APP_VERSION=1.0.0
DEBUG=false

# 服务器配置
HOST=0.0.0.0
PORT=8000

# 数据库配置
DATA_DIR=/app/data
DB_PATH=/app/data/data.db

# 代理配置（可选，如果需要代理访问外网）
# HTTP_PROXY=http://127.0.0.1:7890
# HTTPS_PROXY=http://127.0.0.1:7890

# RSSHub配置（备选方案）
RSSHUB_URL=http://172.31.240.1:1200

# 定时任务配置（分钟）
FETCH_INTERVAL=10

# 日志配置
LOG_LEVEL=INFO

# 默认用户配置
DEFAULT_INSTAGRAM_USER=kohinata_mika
DEFAULT_TWITTER_USER=kohinatamika

# Twitter API配置（可选，用于优先获取Twitter粉丝数）
# TWITTER_AUTH_TOKEN=your_twitter_auth_token_here

# =============================================================================
# 部署说明：
# 1. 复制此文件为 .env: cp env.example .env
# 2. 根据需要修改配置值
# 3. 使用 docker-compose up -d 启动服务
# =============================================================================